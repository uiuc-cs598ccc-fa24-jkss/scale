get:
  tags:
    - health
  summary: Health Check
  operationId: get_api_health
  responses:
    '200':
      description: "Service is healthy"
      content:
        application/json:
          schema: {}
